{
  "name": "cv-coverletter-agent",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "uvicorn app.main:app --reload",
    "frontend": "npm --prefix web run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
